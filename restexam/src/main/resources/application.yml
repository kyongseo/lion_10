spring:
  application:
    name: restexam

  datasource:
    url: jdbc:mysql://localhost:3306/exampledb
    username: root1234
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
        ddl-auto: update
        show-sql: true

server:
  port: 80