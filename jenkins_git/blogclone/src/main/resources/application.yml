spring:
  application:
    name: blogclone

  datasource:
    url: jdbc:mysql://localhost:3306/db
    username: root1234
    password: root1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true

server:
  port: 80